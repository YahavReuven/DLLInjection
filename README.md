# DLLInjection
Code for injecting a DLL to a process.  
Simply enter the process id to the program.  
The project includes a simple dll which displayes a MessageBox.  
To inject your own DLL replace the `DLL_PATH` define in the `dll_injector.h` module.